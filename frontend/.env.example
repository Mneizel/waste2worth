# "local"  = fully in-browser prototype (default, no backend needed)
# "remote" = talk to the real backend API
VITE_API_MODE=local

# Only used when VITE_API_MODE=remote:
VITE_API_BASE_URL=http://localhost:4000
